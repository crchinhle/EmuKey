# Root Compose only controls ports published on the host.
# Backend runtime configuration, including DATABASE_URL, is loaded from backend/.env.
API_PORT=3000
WEB_PORT=5173
REDIS_PORT=6379
EVM_RPC_PORT=8545
